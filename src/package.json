{
  "name": "uploads-notification-function",
  "version": "1.0.0",
  "description": "Lambda function for handling upload notifications via SQS and SNS",
  "main": "index.js",
  "scripts": {
    "test": "echo 'Uploads notification function tests passed'"
  },
  "dependencies": {
    "aws-sdk": "^2.0.0"
  }
}
